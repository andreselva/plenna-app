server {
    listen ${PORT};
    root /usr/share/nginx/html;
    index index.html;

    location = /sitemap.xml {
        return 404;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}